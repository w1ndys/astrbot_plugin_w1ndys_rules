{
  "forbidden_guideline": {
    "description": "违禁长什么样",
    "type": "string",
    "default": "",
    "hint": "用一段话说明什么样的消息算违禁、什么样的不算。大模型按这份设定判断。"
  },
  "forbidden_samples": {
    "description": "违禁样本",
    "type": "text",
    "default": "",
    "hint": "给模型看的判断样本，可换行。建议每行一条，违禁写「…… -> 是」，正常聊天写「…… -> 否」。"
  },
  "forbidden_mute_seconds": {
    "description": "禁言秒数",
    "type": "int",
    "default": 60,
    "hint": "判定违禁后禁言多久。没填时按 60 秒。"
  },
  "forbidden_remind_text": {
    "description": "提醒文案",
    "type": "string",
    "default": "",
    "hint": "判定违禁后发到群里的提醒。留空则暂时不发群内提醒。"
  },
  "forbidden_feishu_webhook": {
    "description": "飞书 webhook",
    "type": "string",
    "default": "",
    "secret": true,
    "hint": "飞书自定义机器人 webhook，只填在本机 WebUI，不要写进代码仓。留空则发不出飞书通知。"
  },
  "forbidden_block_group_card_groups": {
    "description": "拦截群名片的群",
    "type": "list",
    "default": [],
    "hint": "要拦截 QQ 群名片的群号，按条添加。名单里的群发群名片直接当违规：撤回、禁言、提醒、飞书。不走触发词和模型。空名单则都不拦。群主和管理员不拦。",
    "items": {
      "type": "string"
    }
  },
  "verify_mute_seconds": {
    "description": "入群验证禁言秒数",
    "type": "int",
    "default": 600,
    "hint": "待验证的人发错码时禁言多久。0 表示不禁。没填时按 600 秒。超过 QQ 30 天会夹住。"
  }
}
