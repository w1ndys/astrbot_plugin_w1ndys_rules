# astrbot_plugin_w1ndys_rules

AstrBot 插件：群规。关键词回复、欢迎语、入群验证、邀请树由代码处理；违禁词先由
代码匹配全局触发词，命中后才让当前大模型按固定准则和 WebUI 样本回答「是/否」。
入群验证私聊交码，白天每 2 小时提醒，提醒超过 4 次仍未验证则踢出。

从 monorepo `w1ndys-astrbot-plugins` 拆出，独立成仓。群开关由本插件自己管，
不抽跨插件公共模块。

## 关键词回复

- 群员发一条消息，内容与某个关键词完全相同，机器人就回预设内容。
- 不 @ 机器人、不唤醒，照样回复，也不会触发模型。
- 每个群的开关默认关闭，需要管理员在群里发 `关键词 开` 打开，不用带唤醒前缀。
- 管理员可以用自然语言增删改查关键词，例如「添加关键词 原神 回复 好玩」。这条要带唤醒前缀，因为要进模型。
- 一次贴很多条时用 `关键词 批量`，不经过模型，也不用带前缀。后面换行，一行一条，关键词和回复用 `|` 分开。已有关键词会报冲突，不会覆盖。

## 违禁词

- 每个群的开关默认关闭，管理员在群里发 `违禁词 开` 或 `违禁词 关`，不用唤醒前缀。
- 触发词全局保存在 `rules.db`。管理员在群里带唤醒前缀后，可用自然语言增删改查；只在群会话里改，结果对所有群生效。
- 违禁样本和判断准则在 WebUI：准则是单行说明，样本是可换行文本。不要在 QQ 里粘贴原始样本。
- WebUI 还保存提醒文案、禁言时间和飞书 Webhook。
- 群消息包含任一触发词后才送模型判断；英文匹配不区分大小写。只有模型整句回答「是」才撤回、禁言并提醒。
- WebUI「拦截群名片的群」按群号名单控制，按条添加。写进名单的群发 QQ 群名片直接当违规（撤回、禁言、提醒、飞书），不走触发词和模型，也不依赖「违禁词 开」。空名单则都不拦。群主和管理员不拦。
- 近 7 天在本群发过言的人，即使命中触发词也不送模型。刚装上没有历史，全员先当不活跃。
- WebUI 测试页读取全局触发词和 WebUI 样本；测试不会执行群内处置。

## 欢迎语

- 每个群的开关默认关闭，管理员在群里发 `欢迎语 开` 或 `欢迎语 关`，不用唤醒前缀。
- 设置文案发 `欢迎语 设置`，同一行或换行写下正文。整句发 `欢迎语` 可查看当前文案。
- 打开开关后，有人入群会发送已设置的文案；还没设过则发送「欢迎入群~」。
- 和入群验证同时开时，欢迎语和验证说明分开发；通过验证只撤回验证说明，不撤欢迎语。

## 入群验证

- 每个群的开关默认关闭，管理员在群里发 `入群验证 开` 或 `入群验证 关`，不用唤醒前缀。
- 打开后，有人入群会立刻禁言 30 天，并在群里单独发验证码，要求私聊我交码。
- 私聊消息里带着这串码就算通过；码前后不能是数字，避免把更长号码里的一段当成交码。复制整段说明也可以。群里发码不算。
- 自己交码通过后：解禁、撤回群里的验证提醒，并**私聊**回「已通过人机验证。」，群里不再报通过。对不上就私聊回「验证码不对。」
- 仍处于 pending 时，入群后每 2 小时群里再提醒一次并换新码（旧码立刻作废）。入群那条说明不算提醒。只在北京时间 8:00–22:00 发（含 8 点，不含 22 点）；夜里到期的等到早上 8 点再发，夜里不换码、不踢人。每次提醒写明这是第几次、还剩几次机会，发出后撤回上一条。
- 白天提醒已满 4 次仍未验证，下一次到期不再发码，直接移出群（不拒绝以后再入群），撤回上一条提醒，并在群里说明已移出。踢失败则保留 pending，下一拍再试。
- 停机期间错过的提醒不连发，重启后只补当前这一档。
- 同时多个群待验证时，按码匹配对应那一群。图片、语音不当交码。
- 群消息仍先走违禁词。待验证的人若还能发言，广告也会撤回。
- 管理员带唤醒前缀后，可用自然语言扫描、通过、拒绝本群待验证的人。扫描会在群里 @；通过会解禁、撤回提示，并在群里 @ 该成员；拒绝只关 pending、撤回提示，不踢人。
- 人退群或被踢会清掉 pending 并撤回当前提醒。其他管理员解开禁言视为通过，并撤回提示；机器人自己解禁或禁言到期不算。
- 管理员拒绝、拉黑都不踢人。只有验证提醒超过 4 次会踢出。

## 邀请树

- 每个群的开关默认关闭，管理员在群里发 `邀请树 开` 或 `邀请树 关`，不用唤醒前缀。
- 打开后，入群会记下「谁拉了谁」；退群不删边，再进覆盖成最新邀请人。
- 管理员带唤醒前缀后，可查某人的上线链（一直往上追）和整条下线（递归整棵树）。

## 一个必须知道的坑

AstrBot 在唤醒检查阶段会把唤醒前缀从消息开头剥掉。如果某个部署把唤醒前缀配成
「卷卷」，那么群员发「卷卷你好」到最后只剩「你好」参与匹配。因此**关键词不能以
唤醒前缀开头**，否则它永远匹配不到。插件在写入时会拦下这类关键词并说明原因。

## 匹配口径

整条消息完全相等才算命中，不做包含匹配，也不放宽大小写和空白。匹配用的是
AstrBot 解析后的纯文本，不是 OneBot 原始 `raw_message`，所以带 @ 或图片的消息
只比对文本部分。

## 开发

改完插件后需要让 AstrBot 重新加载。注意 AstrBot 的热重载被环境变量守卫：

```python
if os.getenv("ASTRBOT_RELOAD", "0") == "1":
    asyncio.create_task(self._watch_plugins_changes())
```

没设 `ASTRBOT_RELOAD=1` 时文件监视根本不会启动，改代码必须重启容器才生效。
